### 実装したいサービス
- WebGISの画面上でユーザーが指定した任意の線分について、複数の時期のDEMの断面図を作成する。
- 対象地域は2024年1月の地震と2024年９月の豪雨で大きな地形変化があった能登半島北部とする。
- 作成した断面図はPNG形式のファイルでダウンロード可能なものにする。
- WebGISの画面には対象御地域についてタイル配信されているマップを、タイルのアドレスを指定して参照できるようにする。
- 断面図を作成したラインをオーバーレイしたマップをPNG形式でダウンロードできるようにする。マップ画面には縮尺を表示する。

## 実装のイメージ
- まずは、github Pages上で軽量なサービスとして実装したい。
- DEMはCOG形式のものをファイルとして置いて参照する。空間参照はEPSG:6675を想定する。DEMの解像度は0.5mのものを想定する。
- Leafletで表示したWebGIS上の画面からユーザーが指定する、線分の開始点と終了点の座標をWGS84の緯度経度形式で読み取る。
- 線分の開始点と終了点の座標をEPSG:6675にメートル単位で変換する。
- ユーザーは測線上のDEMのサンプリング間隔をメートル単位で指定する。
- 線分の座標とサンプリング間隔を元に、複数のDEMから端点からの距離(d)と標高(z)の配列を生成する。
- d, z の値から断面図を作成する。